<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarsApp">
        <activity android:name=".EntranceTestActivity"></activity>
        <activity android:name=".CourseVideoListActivity" />
        <activity android:name=".CourseListActivity" />
        <activity android:name=".ActivityPayment" />
        <activity android:name=".ActivityFaceRecognition" />
        <activity android:name=".ActivityCertificate" />
        <activity android:name=".VideoViewActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>